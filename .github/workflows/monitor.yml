name: Actions queue diagnostic
on:
  push:
    branches: [fix/actions-queue]
    paths: [".github/workflows/monitor.yml"]
  workflow_dispatch:

permissions: {}

jobs:
  runner-check:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Confirm GitHub can start a job
        run: date -u
